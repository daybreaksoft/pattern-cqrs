@{
    ViewData["Title"] = "Books";
}
<h2>Books</h2>
<hr />
<section>
    <a class="btn btn-default" asp-action="Edit">Create</a>
</section>
<section>
    <table class="table table-striped">
        <thead>
        <tr>
            <th>Name</th>
            <th>Sex</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        @*@foreach (var author in ViewBag.Authors)
        {
            <tr>
                <td>@author.Name</td>
                <td>@author.Sex</td>
                <td>
                    <a class="btn btn-default btn-sm" asp-action="Edit" asp-route-id="@author.Id">Edit</a>
                    <a class="btn btn-danger btn-sm" asp-action="DeleteCommand" asp-route-id="@author.Id">Delete</a>
                </td>
            </tr>
        }*@
        </tbody>
    </table>
</section>